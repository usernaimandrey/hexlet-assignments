<h1>Tasks</h1>
<%= link_to 'New task', new_task_path %>
<% @tasks.each do |task| %>
<h2><%= link_to task.name, task_path(task) %></h2>
<p><%= task.creator %></p>
<p><%= task.status %></p>
<small><%= task.created_at.strftime("%d-%m-%Y %H:%M:%S") %></small>
<hr>
<% end %>
